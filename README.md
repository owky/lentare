# Environment

    $ npm -v
    10.8.3
    $ node -v
    v22.9.0
    $ vue --version
    @vue/cli 5.0.8

# Vite + Vue + Vuetify

    $ npm create vuetify@latest
    $ cd lentare
    $ npm install
    $ npm run dev
